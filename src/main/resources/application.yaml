spring:
  application.name: todo-list
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:todolist_dev
    username: sa
    password:
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
    open-in-view: false
